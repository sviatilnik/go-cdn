# Go CDN

Простой и эффективный CDN (Content Delivery Network) сервер на Go для хранения и раздачи файлов.

## Возможности

- 📤 **Загрузка файлов** - загрузка файлов через HTTP API
- 📥 **Получение файлов** - доступ к файлам по URL
- 🗑️ **Удаление файлов** - удаление файлов через API
- 💾 **Множественные хранилища** - поддержка файловой системы и S3-совместимых хранилищ (Yandex Object Storage)
- 🗜️ **Сжатие** - автоматическое gzip сжатие ответов
- ⏱️ **Context поддержка** - корректная обработка отмены операций и таймаутов
- 🚀 **Graceful shutdown** - корректное завершение работы сервера

## Структура проекта

```
go-cdn/
├── cmd/
│   └── server/
│       └── main.go          # Точка входа приложения
├── internal/
│   ├── config/              # Конфигурация
│   ├── httphandlers/        # HTTP обработчики
│   │   ├── savefile.go      # Загрузка файлов
│   │   ├── getfile.go       # Получение файлов
│   │   └── deletefile.go    # Удаление файлов
│   ├── middlewares/         # HTTP middleware
│   │   └── compress.go      # Gzip сжатие
│   ├── server/              # HTTP сервер
│   └── storage/             # Абстракция хранилища
│       ├── storage.go       # Интерфейс Storage
│       ├── fs.go            # Реализация для файловой системы
│       └── s3.go            # Реализация для S3
└── files/                   # Директория для хранения файлов (FS storage)
```

## Требования

- Go 1.25.1 или выше
- Для S3 storage: AWS credentials или Yandex Cloud credentials

## Установка

```bash
git clone https://github.com/sviatilnik/go-cdn.git
cd go-cdn
go mod download
```

## Использование

### Запуск с файловым хранилищем (по умолчанию)

```bash
go run cmd/server/main.go
```

Сервер запустится на порту `:8080` и будет использовать файловую систему для хранения файлов в директории `./files`.

### Запуск с S3 хранилищем

Отредактируйте `internal/server/server.go`:

```go
// Раскомментируйте:
storage, err := storage.NewS3Storage(context.Background())
if err != nil {
    log.Fatalf("failed to create s3 storage: %v", err)
}

// Закомментируйте:
// storage := storage.NewFSStorage("./files")
```

Настройте AWS credentials через переменные окружения или файл `~/.aws/credentials`:

```bash
export AWS_ACCESS_KEY_ID=your_access_key
export AWS_SECRET_ACCESS_KEY=your_secret_key
export AWS_REGION=ru-central1
```

## Хранилища

### FS Storage (Файловая система)

Файлы сохраняются в локальной файловой системе с организацией по хеш-директориям для равномерного распределения.

**Особенности:**
- Автоматическое создание директорий
- Организация файлов по хешу для оптимизации
- UUID для имен файлов

### S3 Storage (Yandex Object Storage)

Поддержка S3-совместимых хранилищ, включая Yandex Object Storage.

**Особенности:**
- Автоматическое создание bucket'ов
- Поддержка кастомных endpoint'ов
- Регион: `ru-central1` (по умолчанию)

**Настройка:**
- Endpoint: `https://storage.yandexcloud.net`
- Регион: `ru-central1`

## Технологии

- [Chi](https://github.com/go-chi/chi) - легковесный HTTP роутер
- [AWS SDK v2](https://github.com/aws/aws-sdk-go-v2) - для работы с S3
- [UUID](https://github.com/google/uuid) - генерация уникальных идентификаторов

## Разработка

### Сборка

```bash
go build -o bin/server cmd/server/main.go
```

### Запуск

```bash
./bin/server
```

## Планы развития

### Ближайшие планы

~~- 📝 **Провайдеры конфигурации** - добавление возможности конфигурации с помощью yaml.~~
- 🔐 **Аутентификация и авторизация** - добавление JWT токенов и RBAC для контроля доступа к файлам
- 📊 **Метрики и мониторинг** - интеграция Prometheus метрик и health check endpoints
- 🚦 **Rate limiting** - ограничение частоты запросов для защиты от злоупотреблений
- 📝 **Метаданные файлов** - поддержка кастомных метаданных и тегов для файлов
- 🔄 **Версионирование файлов** - возможность хранить несколько версий одного файла

### Среднесрочные планы

- 💾 **Дополнительные хранилища** - поддержка MinIO
- ⚡ **Кэширование** - интеграция Redis для кэширования метаданных и часто запрашиваемых файлов
- 📦 **Batch операции** - массовая загрузка и удаление файлов
- 🔔 **Webhooks** - уведомления о событиях (загрузка, удаление файлов)
- 🗄️ **База данных** - использование PostgreSQL/MySQL для хранения метаданных и индексации

### Долгосрочные планы

- 🔍 **Поиск и фильтрация** - полнотекстовый поиск по метаданным и содержимому файлов
- 📈 **Аналитика** - статистика использования, популярные файлы, трафик
- 🔄 **Миграция между хранилищами** - инструменты для переноса данных между разными типами хранилищ
- 🧪 **Тестирование** - расширенное покрытие unit и integration тестами
- 📚 **OpenAPI документация** - автоматическая генерация Swagger/OpenAPI спецификации

## Лицензия

MIT
